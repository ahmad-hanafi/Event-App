export const SET_EVENTS = (state, events) => {
    state.events = events
}

export const SET_EVENTBYID = (state, event) => {
    state.event = event
}

export const SET_CONFIRMEVENT = (state, confirmEvent) => {
    state.confirmEvent = confirmEvent
}